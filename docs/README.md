# Diagrams

The main `README.md` references two images from this folder:

- `pipeline.png` — the top‑to‑bottom security pipeline
  (Untrusted world → Ingestion → Trust boundary → LLM engine →
  Structured proposal → CAPITA security layer → Decision engine →
  Financial engine → Tamper‑evident audit log).
- `architecture.png` — the "System Architecture — from intent to action"
  diagram (AI engine → CAPITA control plane → grounding policy / trust →
  action decision → ALLOW / BLOCK / ESCALATE).

**Save the two diagram images here with exactly those filenames**
(`docs/pipeline.png` and `docs/architecture.png`) so they render in the README.
