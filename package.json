{
  "name": "Capita",
  "version": "1.0.0",
  "private": true,
  "description": "Capita -deterministic AI financial security. Premium landing page + real backend.",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
